# program Create a function to calculate simple interest.
#.....................................................................
# function to calculate simple interest
def si(p, r, t):
    return (p*r*t)/100

print(si(float(input()), float(input()), float(input())))
# ....................................................................
"""
Output:
100.0
"""